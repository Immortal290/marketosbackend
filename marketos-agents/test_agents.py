import os
import sys
from dotenv import load_dotenv

# Load env variables provided by the user in the prompt (as we wrote it to .env locally)
# First we need to make sure the .env is up to date with what the user provided.
